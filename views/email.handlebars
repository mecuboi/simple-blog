<div class="mt-3 md:col-span-2 md:mt-0">

    <div class="contact">
        <h2 class="text-center text-l text-orange-700 font-bold">Let the seller know that you are an Interested !!</h2>
        <h2 id="sent" class="text-center  text-xl text-orange-500 font-bold "> {{msg}}</h2>
        <form method="POST" action="send">
            <p>
                <label class="block text-orange-500 text-m text font-bold mb-2 border-round">Send To</label>
                <input type="email" name="receiver" value="{{sendEmailTo}}" text="{{sendEmailTo}}">
            </p>

            <hr>


            <p>
                <label class="block text-orange-500 text-m font-bold mb-2">Sender's Contact details:</label>

            </p>
            <hr>
            <p>
                <label class="block text-orange-500  text-sm font-bold mb-2">From</label>
                <input type="text" name="name" value="{{activeUser.first_name}}">
            </p>
            <p>
                <label class="text-orange-500  text-sm font-bold mb-2">Email Address</label>
                <input type=" email" name="email" value="{{activeUser.email}}">
            </p>
            <p>
                <label class="block text-orange-500  text-sm font-bold mb-2">Phone Number</label>
                <input type=" text" name="phone" value="{{activeUser.phone_number}}">
            </p>
            <p class=" full">
                <label class="block text-orange-500  text-m font-bold mb-2">Message</label>
                <textarea class="text-orange-500" name="message"
                    rows="5">I am super interested in your Ad :) !!</textarea>
            </p>
            <p>
                <button
                    class="transition center duration-500 bg-orange-300 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                    type="submit">Submit</button>
            </p>
        </form>

    </div>